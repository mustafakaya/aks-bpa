apiVersion: v1
kind: ConfigMap
metadata:
  name: aks-bpa-config
  namespace: aks-bpa
  labels:
    app.kubernetes.io/name: aks-bpa
data:
  DATABASE_URL: "sqlite+aiosqlite:///./data/aks_bpa.db"
  CORS_ORIGINS: '["http://localhost:3000","http://localhost:5173","*"]'
